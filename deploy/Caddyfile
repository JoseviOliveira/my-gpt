{
  log {
    output file {$APP_DIR}/log/caddy.log
    format json
  }
}

josevi.ddns.net {
  # optional: let’s encrypt account email for notices
  # acme_ca https://acme-v02.api.letsencrypt.org/directory
  # email you@example.com

  encode zstd gzip
  # HSTS (optional, enable after first success)
  # header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

  # pass through Basic Auth headers to Flask (you already enforce auth there)
  reverse_proxy 127.0.0.1:4200 {
    header_up X-Forwarded-Proto {scheme}
    header_up X-Forwarded-For {remote_host}
    header_up Host {host}
  }
}
